---
kind: Secret
apiVersion: v1
metadata:
  name: redis-secret
  namespace: redis
type: Opaque
stringData:
  redis.conf: |
    port 6379
    appendonly yes
    protected-mode no
    bind 0.0.0.0
    requirepass admin
